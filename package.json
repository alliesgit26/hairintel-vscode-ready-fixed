{
  "name": "hairintel-vscode-ready",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "android:prepare": "node scripts/prepare-android-assets.js",
    "android:sync": "npm run android:prepare && npx cap sync android",
    "android:open": "npx cap open android",
    "android:add": "npm run android:prepare && npx cap add android",
    "android:build:aab": "cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@supabase/supabase-js": "^2.49.1",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "sharp": "^0.34.5",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
